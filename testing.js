/**
 * @param {Function[]} functions
 * @return {Function}
 */
var compose = function(functions) {
    return functions
};

console.log(d1 = compose(2))

/**
 * const fn = compose([x => x + 1, x => 2 * x])
 * fn(4) // 9
 */